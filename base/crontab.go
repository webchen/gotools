package base

/*
import (
	"time"

	"github.com/robfig/cron/v3"
)

var cronJobObj *cron.Cron

func init() {
	nyc, _ := time.LoadLocation("Asia/Shanghai")
	cronJobObj = cron.New(cron.WithSeconds(), cron.WithLocation(nyc))
	//go cronJobObj.Start()
	go cronJobObj.Start()
	defer cronJobObj.Stop()
}

// AddCronJob 添加系统定时任务
func AddCronJob(spec string, cmd func()) {
	cronJobObj.AddFunc(spec, cmd)
}
*/
